

// static dynamic getDom(Document document, selector) {
// 通过 [1,2,3,4,5] 的方式 document.querySelector().child[0].child[1].child[2]
// }

/// TODO  实现通过文本获取元素
// static int mrOrderStatus(Document document) {
//   var mr = document.querySelectorAll('div.produt-list')[0];
//   document.e
//   mr.getElements();
//   return val;
// }
//   function getElementsByText(str, tag = 'a') {
//   return Array.prototype.slice.call(document.getElementsByTagName(tag)).filter(el => new RegExp(str, 'gi').test(el.textContent.trim()));
//   elem.innerHTML = elem.innerHTML.replace(new RegExp(BOSS, 'g'), `<font size="30px" color="red">${BOSS}</font>`);
//   });
// }
//   function getElementsByText(str, tag = 'a') {
//   return Array.prototype.slice.call(document.getElementsByTagName(tag)).filter(el => el.textContent === str);
//   }
//   function getElementsByText(str, tag = 'a') {
//   return Array.prototype.slice.call(document.getElementsByTagName(tag)).filter(el => el.textContent.trim() === str.trim());